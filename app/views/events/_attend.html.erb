<%= form_for(current_user.invites.build(attendee_event_id: @event.id)) do |f| %>
  <div><%= f.hidden_field :attendee_event_id %></div>
  <%= f.submit "Attend", class: "btn btn-large btn-primary" %>
<% end %>